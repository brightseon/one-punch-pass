import { SectionType } from '../types';
import keys from '../keys';
import names from '../names';

const { SUMMARY_KEY : {  } } = keys;
const { SUMMARY_NAME : {  } } = names;

const SUMMARY_SECTION : SectionType[] = [];

export default SUMMARY_SECTION;