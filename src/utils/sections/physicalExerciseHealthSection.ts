import { SectionType } from '../types';
import keys from '../keys';
import names from '../names';

const { PHYSICAL_EXERCISE_HEALTH_KEY : {  } } = keys;
const { PHYSICAL_EXERCISE_HEALTH_NAME : {  } } = names;

const PHYSICAL_EXERCISE_HEALTH_SECTION : SectionType[] = [];

export default PHYSICAL_EXERCISE_HEALTH_SECTION;