import { SectionType } from '../types';
import keys from '../keys';
import names from '../names';

const { ART_EXPERIENCE_KEY : {  } } = keys;
const { ART_EXPERIENCE_NAME : {  } } = names;

const ART_EXPERIENCE_SECTION : SectionType[] = [];

export default ART_EXPERIENCE_SECTION;