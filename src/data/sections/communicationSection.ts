import { SectionType } from '../../types/types';
import keys from '../keys';
import names from '../names';

const { COMMUNICATION_KEY : {  } } = keys;
const { COMMUNICATION_NAME : {  } } = names;

const COMMUNICATION_SECTION : SectionType[] = [];

export default COMMUNICATION_SECTION;