import { SectionType } from '../../utils/types';
import keys from '../keys';
import names from '../names';

const { SOCIAL_RELATIONS_KEY : {  } } = keys;
const { SOCIAL_RELATIONS_NAME : {  } } = names;

const SOCIAL_RELATIONS_SECTION : SectionType[] = [];

export default SOCIAL_RELATIONS_SECTION;