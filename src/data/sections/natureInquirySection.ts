import { SectionType } from '../../utils/types';
import keys from '../keys';
import names from '../names';

const { NATURE_INQUIRY_KEY : {  } } = keys;
const { NATURE_INQUIRY_NAME : {  } } = names;

const NATURE_INQUIRY_SECTION : SectionType[] = [];

export default NATURE_INQUIRY_SECTION;